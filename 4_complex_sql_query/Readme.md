# SQL-script - complex SQL query combining multiple simulation outputs

This folder contains a SQL-query example that asks the database to return buildings that do not comply to solar access and noise thresholds and whose
"class" is "habitation" (i.e., residential buildings) or "healthcare" (e.g., hospitals).
